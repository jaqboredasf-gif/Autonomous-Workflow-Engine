{
  "name": "exattime",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "_engines_note": "24, not 20: the purchasing store is node:sqlite, which is part of the runtime rather than a dependency. On Node 20 the import fails at startup with a module-not-found error that names nothing anybody can act on. The Dockerfile already pins 24; this is the same fact stated where a developer installing on a laptop will meet it.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "pcc:dev": "bash scripts/pcc-dev.sh",
    "pcc:dev-user": "node scripts/provision-local-tenants.mjs",
    "pcc:e2e": "node scripts/eval-purchasing-e2e.mjs",
    "proof": "node scripts/eval-proof.mjs",
    "proof:case-study": "node scripts/proof-case-study.mjs",
    "programs": "node scripts/eval-programs.mjs",
    "readiness": "node scripts/iic-readiness.mjs",
    "deployment-gate": "node scripts/pcc-deployment-gate.mjs",
    "rehearse": "bash scripts/rehearse-locally.sh"
  },
  "devDependencies": {
    "next": "16.2.10"
  }
}
